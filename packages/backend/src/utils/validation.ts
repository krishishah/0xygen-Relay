import { SchemaValidator, ValidatorResult, schemas } from '@0xproject/json-schemas';

export class ValidationUtils {

    public static isValidSignedOrderJSON(signedOrderStr: string): boolean {
        return false;
    } 
}